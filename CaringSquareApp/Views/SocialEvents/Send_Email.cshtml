@model CaringSquareApp.Models.SendEmailViewModel

@{
    ViewBag.Title = "Send Email To Friend";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />
<br />
<br />
<div class="container" style="margin-left:80px;">
    <h2>Share your Event by Email</h2>


    @using (Html.BeginForm("Send_Email", "SocialEvents", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()


        if (@ViewBag.Result != null)
        {
            <div class="alert alert-info">@ViewBag.Result</div>
        }

        <div class="form-horizontal">
            <h4>SendEmailViewModel</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                @Html.LabelFor(model => model.ToEmail, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.ToEmail, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.ToEmail, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.Subject, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Subject, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Subject, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.Contents, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    <textarea style="height:300px;" class="form-control" cols="20" data-val="true" data-val-required="Please enter the contents" id="Contents" name="Contents" rows="2">
                
            </textarea>
                    @Html.ValidationMessageFor(model => model.Contents, "", new { @class = "text-danger" })
                </div>
            </div>



            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" style="background-color: #94d16a; margin-left:10px; color: #000000;" value="Send" class="btn btn-default" />
                </div>
            </div>
        </div>
    }

    <div>
        @Html.ActionLink("Back to Event Details Page", "Index")
    </div>
    </div>
    <script>
        // document.getElementById("result").innerHTML = localStorage.getItem("textvalue")



        function init() {
            var result = document.getElementById('Contents');

            result.value = `Hello Friend,
     Please find the details of the created Event. I would be happy if you could join me for the same.
     Regards,
     User`;

            var text = "";

            try {
                var textArray = JSON.parse(localStorage.getItem('__pageCache'));
                textArray = textArray[0].split('\n').map(g => g.trim()).filter(Boolean);


                for (var i = 0; i <= textArray.length; i += 2) {
                    text += `${textArray[i]}:  ${textArray[i + 1]} \n`

                }
            } catch (err) {
            }
            // console.log(textArray);
            // var tempHtml = textArray.map(text => `${text}`).join('');
            // var textHtml = textArray.map(text => `<p>${text}</p>`).join('');
            //console.log(tempHtml);

            console.log(text)

            result.value += `\n ${text}`

        }


        window.addEventListener('DOMContentLoaded', event => {
            init();
        });
    </script>

    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
        <script>
            tinymce.init({
                selector: '#Contents'
            });
        </script>
    }
